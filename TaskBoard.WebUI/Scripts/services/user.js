(function (define, require) {
    define(['jquery'], function ($) {
        var service = function () {
            var self = this;
        };
        return new service();
    });
})(window.define, window.require);