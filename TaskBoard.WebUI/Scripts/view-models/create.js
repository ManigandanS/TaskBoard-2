(function (define, require) {
    define(['ko', 'services/project'],
        function(ko) {
            return function () {
                var self = this;
            }
        });
})(window.define, window.require);